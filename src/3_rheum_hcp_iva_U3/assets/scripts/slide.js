'use strict';

$(() => {

//private
var el, touchclick, csmsBtn, closeBtn;

class Slide {

	constructor() {
		//set vars
      // el = $('#rheum_hcp_iva_dm_pm_study_design');
      // csmsBtn = el.find('.csms-popup-btn');
      // closeBtn = el.find('#csms .close');
      // touchclick =
      //    'ontouchend' in document.documentElement
      //       ? 'touchend'
      //       : window.navigator.pointerEnabled
      //       ? 'pointerup'
      //       : 'click';

      // csmsBtn.on(touchclick, (e) => {
      //    const tracking = {
      //       id: 'button',
      //       type: 'DM/PM study design: core set measures definition (blue dotted line)',
      //       description: 'dm_pm_study_design>slide_dm_pm_studies>study design_CSMs definition link'
      //    };
      //    veevaUtils.trackEvent(tracking, () => {});
      // });

      // closeBtn.on(touchclick, (e) => {
      //    const tracking = {
      //       id: 'button',
      //       type: 'DM/PM study design: core set measures definition pop up X',
      //       description: 'dm_pm_study_design>slide_dm_pm_studies>study design_CSMs pop up close'
      //    };
      //    veevaUtils.trackEvent(tracking, () => {});
      // });

      //initial setup

      //listeners
	}

	//----- handlers ------//


	//----- utils ------//

}

var slide = new Slide();

});
